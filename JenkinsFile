node {
    def app

    stage('Clone repository') {
      

        checkout scm
    }

    stage('Update GIT') {
            script {
                catchError(buildResult: 'SUCCESS', stageResult: 'FAILURE') {
                    withCredentials([usernamePassword(credentialsId: 'github', passwordVariable: 'GIT_PASSWORD', usernameVariable: 'GIT_USERNAME')]) {
                        //def encodedPassword = URLEncoder.encode("$GIT_PASSWORD",'UTF-8')
                        sh "git config user.email parnal.patil@gmail.com"
                        sh "git config user.name ParnalPatil"
                        //sh "git switch master"
                        sh "cat mongo-secret.yaml"
                        sh "cat mongoDeployment.yaml"
                        sh "cat registry-service-deployment.yaml"
                        sh "sed -i 's+parnal/codist-registry.*+parnal/codist-registry:amd64+g' mongo-secret.yaml"
                        sh "sed -i 's+parnal/codist-registry.*+parnal/codist-registry:amd64+g' mongoDeployment.yaml"
                        sh "sed -i 's+parnal/codist-registry.*+parnal/codist-registry:amd64+g' registry-service-deployment.yaml"
                        sh "kubectl --kubeconfig /home/exouser/.kube/config apply -f mongo-secret.yaml"
                        sh "kubectl --kubeconfig /home/exouser/.kube/config apply -f mongoDeployment.yaml"
                        sh "kubectl --kubeconfig /home/exouser/.kube/config apply -f registry-service-deployment.yaml"
                        sh "git add ."
                        sh "git commit -m 'Done by Jenkins Job changemanifest'"
                        sh "git push https://${GIT_USERNAME}:${GIT_PASSWORD}@github.com/airavata-courses/CoDist.git HEAD:dev-registry-kube"
      }
    }
  }
}
}